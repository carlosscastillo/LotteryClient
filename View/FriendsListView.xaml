<Page x:Class="Lottery.View.FriendsListView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      xmlns:language="clr-namespace:Lottery.Properties.Langs"
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#F0F0F0"
      Title="FriendsListView">

    <Grid>
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Width="500">

            <TextBlock Text="{x:Static language:Lang.FriendsListLabelTitle}"
                FontSize="28"
                FontWeight="Bold"                   
                TextAlignment="Center"
                Margin="0 0 0 60"/>

            <Button Content="{x:Static language:Lang.InviteFriendButton}"
                    Style="{StaticResource BlueLargeButtonStyle}"
                    HorizontalAlignment="Center"
                    Margin="0 0 0 20"/>
            
            <ItemsControl ItemsSource="{Binding Friends}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Height="40" Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            
                            <TextBlock Text="{Binding Name}"
                                       VerticalAlignment="Center"
                                       FontSize="14"/>

                            <Button Content="{x:Static language:Lang.DeleteFriendButton}"
                                    Grid.Column="1"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Width="80"
                                    Height="30"
                                    Command="{Binding DataContext.DeleteFriendCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                    CommandParameter="{Binding}" />
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <Button Content="{x:Static language:Lang.GlobalButtonBack}"
                    Style="{StaticResource PrimaryButtonStyle}"
                    HorizontalAlignment="Center"
                    Width="80"
                    Height="30"
                    Margin="0 150 0 0"/>
        </StackPanel>
    </Grid>
</Page>
