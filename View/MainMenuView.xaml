<Window x:Class="Lottery.View.MainMenuView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.View"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Lottery.Properties.Langs"
        Title="Main Menu" Height="350" Width="500"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style x:Key="SideIconButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Static ToolBar.ButtonStyleKey}}">
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Margin" Value="0,15,0,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="SideIconTextStyle" TargetType="TextBlock" BasedOn="{StaticResource IconStyle}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Images/background.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Border BorderBrush="#55FFFFFF" BorderThickness="1" Background="#AA000000" CornerRadius="12" Padding="30"
                MaxWidth="750" MaxHeight="500"
                VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.3" BlurRadius="15"/>
            </Border.Effect>

            <Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,0,40,0">
                        <Button Content="{x:Static language:Lang.CreateLobbyLabelTitle}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="50,0,0,20"/>
                        <Button Content="{x:Static language:Lang.JoinLobbyLabelTittle}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="50,0,0,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="20,0,0,0">
                        <Button Style="{StaticResource SideIconButtonStyle}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE713;" Style="{StaticResource SideIconTextStyle}" Margin="21,0,0,0"/>
                                <TextBlock Text="{x:Static language:Lang.MainMenuButtonSettings}" FontSize="10"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SideIconButtonStyle}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE716;" Style="{StaticResource SideIconTextStyle}" Margin="6,0,0,0"/>
                                <TextBlock Text="{x:Static language:Lang.MainMenuButtonFriends}" FontSize="10"/>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource SideIconButtonStyle}">
                            <StackPanel Orientation="Vertical" HorizontalAlignment="Center">
                                <TextBlock Text="&#xE77B;" Style="{StaticResource SideIconTextStyle}" Margin="1,0,0,0"/>
                                <TextBlock Text="{x:Static language:Lang.MainMenuButtonProfile}" FontSize="10"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>