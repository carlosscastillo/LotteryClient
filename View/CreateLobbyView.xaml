<Window x:Class="Lottery.View.CreateLobbyView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.View"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Lottery.Properties.Langs"
        Title="CreateLobbyView" Height="600" Width="400"
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border BorderBrush="#DDDDDD" 
            BorderThickness="1" 
            Background="White" 
            CornerRadius="12" 
            Padding="40" 
            MaxWidth="420"
            VerticalAlignment="Center" 
            HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="{x:Static language:Lang.CreateLobbyLabelTitle}" 
                       Style="{StaticResource TitleTextStyle}" 
                       Margin="0,0,0,10"/>

                <Label Content="{x:Static language:Lang.CreateLobbyLabelGameMode}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=GameModeComboBox}"/>
                <ComboBox x:Name="GameModeComboBox">
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeStandard}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeCenter}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeDiagonals}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeCorners}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeFrame}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeMegaLottery}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelGameModeUnfairLottery}"/>
                </ComboBox>

                <Label Content="{x:Static language:Lang.CreateLobbyLabelSound}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=SoundComboBox}"/>
                <ComboBox x:Name="SoundComboBox">
                    <ComboBoxItem Content="{x:Static language:Lang.GlobalTagYes}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.GlobalTagNo}"/>
                </ComboBox>

                <Label Content="{x:Static language:Lang.CreateLobbyLabelCardImage}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=CardImageComboBox}"/>
                <ComboBox x:Name="CardImageComboBox">
                    <ComboBoxItem Content="{x:Static language:Lang.GlobalTagYes}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.GlobalTagNo}"/>
                </ComboBox>

                <Label Content="{x:Static language:Lang.CreateLobbyLabelCardDisplayTime}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=CardTimeComboBox}"/>
                <ComboBox x:Name="CardTimeComboBox">
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelCardDisplayOneSecond}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelCardDisplayTwoSeconds}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelCardDisplayThreeSeconds}"/>
                </ComboBox>

                <Label Content="{x:Static language:Lang.CreateLobbyLabelMarkers}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=ChipTypeComboBox}"/>
                <ComboBox x:Name="ChipTypeComboBox">
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelMarkersBeans}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelMarkersBottleCaps}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelMarkersPaperBalls}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelMarkersCoins}"/>
                    <ComboBoxItem Content="{x:Static language:Lang.CreateLobbyLabelMarkersCorn}"/>
                </ComboBox>

                <Button x:Name="CreateLobbyButton" Click="CreateLobbyButton_Click"
                        Content="{x:Static language:Lang.CreateLobbyLabelTitle}"
                        Margin="0,40,0,0" Padding="12" FontSize="16" FontWeight="Bold" Foreground="White" Background="Black" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
