<Window x:Class="Lottery.View.User.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.View"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Lottery.Properties.Langs"
        xmlns:helpers="clr-namespace:Lottery.Helpers"
        Title="Login" 
        WindowStartupLocation="CenterScreen"
        WindowState="Maximized"
        AllowsTransparency="True" WindowStyle="None" Background="Transparent">

    <Grid>

        <Grid.Background>
            <ImageBrush ImageSource="/Images/background.jpg" Stretch="UniformToFill"/>
        </Grid.Background>

        <Button Content="X"
                HorizontalAlignment="Right"
                VerticalAlignment="Top"
                Margin="0,10,10,0"
                Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"
                Foreground="White"
                FontWeight="Bold"
                FontSize="14"
                Click="CloseButton_Click"/>

        <Border BorderBrush="#55FFFFFF" BorderThickness="1" Background="#AA000000" CornerRadius="12" Padding="40" MaxWidth="420" VerticalAlignment="Center" HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
            </Border.Effect>

            <StackPanel>
                <TextBlock Text="{x:Static language:Lang.LoginLabelTitle}" 
                           Style="{StaticResource TitleTextStyle}" 
                           Foreground="White"/>
                <TextBlock Text="{x:Static language:Lang.LoginLabelEnterDetail}" 
                           Style="{StaticResource SubtitleTextStyle}" 
                           Foreground="#DDDDDD"/>

                <Label Content="{x:Static language:Lang.GlobalLabelNickname}" 
                       Style="{StaticResource LabelStyle}" 
                       Target="{Binding ElementName=NicknameTextBox}" 
                       Foreground="White"/>
                <Grid>
                    <TextBox x:Name="NicknameTextBox" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="&#xE77B;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelPassword}" 
                       Style="{StaticResource LabelStyle}" 
                       Target="{Binding ElementName=PasswordBox}" 
                       Foreground="White"/>
                <Grid>
                    <PasswordBox x:Name="PasswordBox"
                                 helpers:PasswordBoxHelper.BoundPassword="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Button x:Name="StartButton"
                        Content="{x:Static language:Lang.GlobalButtonStart}" 
                        Margin="0,30,0,0" Padding="12" 
                        FontSize="18" FontWeight="Bold" 
                        Foreground="White" Background="Black" 
                        BorderThickness="0" Cursor="Hand"
                        MinWidth="250"
                        Command="{Binding LoginCommand}"
                        CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <TextBlock Text="Iniciando sesión..." HorizontalAlignment="Center" Foreground="#DDDDDD" Margin="0,10,0,0">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsLoggingIn}" Value="True">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Margin="0,20,0,0" HorizontalAlignment="Center" Cursor="Hand">
                    <TextBlock Text="{x:Static language:Lang.LoginButtonForgotMyPassword}" Foreground="#BBBBBB" FontSize="13"/>
                </Button>

                <Button x:Name="SignUpButton"
                        Command="{Binding SignUpCommand}"
                        Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" HorizontalAlignment="Center" Cursor="Hand">
                    <TextBlock Text="{x:Static language:Lang.LoginButtonSignUp}" Foreground="White" FontWeight="SemiBold" FontSize="14"/>
                </Button>

                <Button Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Margin="0,20,0,0" HorizontalAlignment="Center" Cursor="Hand">
                    <TextBlock Text="Iniciar como invitado" Foreground="#BBBBBB" FontSize="13"/>
                </Button>

            </StackPanel>
        </Border>
    </Grid>
</Window>