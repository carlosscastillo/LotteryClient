<Window x:Class="Lottery.View.UserRegisterView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Lottery.View"
        mc:Ignorable="d"
        xmlns:language="clr-namespace:Lottery.Properties.Langs"
        Title="UserRegisterView" Height="700" Width="500"
        Background="#F5F5F5"
        WindowStartupLocation="CenterScreen">
    
    <Border BorderBrush="#DDDDDD" BorderThickness="1" Background="White" CornerRadius="12" Padding="25" MaxWidth="420" VerticalAlignment="Center" HorizontalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="1" Color="Black" Opacity="0.1" BlurRadius="10"/>
        </Border.Effect>

        <Grid>

            <StackPanel x:Name="RegistrationFormPanel" Visibility="Visible">
                <TextBlock Text="{x:Static language:Lang.UserSignUpLabelTitle}" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="{x:Static language:Lang.UserSignUpLabelDetails}" Style="{StaticResource SubtitleTextStyle}"/>

                <Label Content="{x:Static language:Lang.GlobalLabelName}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=NameTextBox}"/>
                <Grid>
                    <TextBox x:Name="NameTextBox" Text="Carlos"/>
                    <TextBlock Text="&#xE77B;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelPaternalLastName}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=PaternalLastNameTextBox}"/>
                <Grid>
                    <TextBox x:Name="PaternalLastNameTextBox" Text="Castillo"/>
                    <TextBlock Text="&#xE77B;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelMaternalLastName}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=MaternalLastNameTextBox}"/>
                <Grid>
                    <TextBox x:Name="MaternalLastNameTextBox" Text="Barradas"/>
                    <TextBlock Text="&#xE77B;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelNickname}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=NicknameTextBox}"/>
                <Grid>
                    <TextBox x:Name="NicknameTextBox" Text="xXCarCasXx"/>
                    <TextBlock Text="&#xE77B;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelEmail}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=EmailTextBox}"/>
                <Grid>
                    <TextBox x:Name="EmailTextBox" Text="carlos@gmail.com"/>
                    <TextBlock Text="&#xE715;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelPassword}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=PasswordBox}"/>
                <Grid>
                    <PasswordBox x:Name="PasswordBox" Password="password"/>
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Label Content="{x:Static language:Lang.GlobalLabelConfirmPassword}" Style="{StaticResource LabelStyle}" Target="{Binding ElementName=ConfirmPasswordBox}"/>
                <Grid>
                    <PasswordBox x:Name="ConfirmPasswordBox" Password="password"/>
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}"/>
                </Grid>

                <Button Content="{x:Static language:Lang.GlobalButtonSignUp}" Command="{Binding RegisterCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Margin="0,15,0,0" Padding="12" FontSize="15" FontWeight="Bold" Foreground="White" Background="Black" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>


            <StackPanel x:Name="VerificationCodePanel" Visibility="Collapsed" HorizontalAlignment="Center">
                <TextBlock Text="{x:Static language:Lang.UserSignUpLabelTitle}" Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="{x:Static language:Lang.GlobalLabelVerificationCode}"  Style="{StaticResource SubtitleTextStyle}"/>

                <TextBlock x:Name="VerificationEmailTextBlock" Text="carlos@gmail.com" FontSize="14" Foreground="Gray" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,2,0,25"/>
                <Label Content="{x:Static language:Lang.GlobalButtonVerificationCode}" FontSize="12" FontWeight="Bold" Margin="0,0,0,5" Target="{Binding ElementName=VerificationCodeTextBox}"/>
                <Grid>
                    <TextBox x:Name="VerificationCodeTextBox" Style="{StaticResource CodeInputFieldStyle}" Text="000000" />
                    <TextBlock Text="&#xE72E;" Style="{StaticResource IconStyle}" Margin="10,0,0,0"/>
                </Grid>
                <Button Content="{x:Static language:Lang.GlobalButtonVerifyCode}"
                    Command="{Binding VerifyCommand}"
                    Margin="0,25,0,0"
                    Padding="12"
                    FontSize="16"
                    FontWeight="Bold"
                    Foreground="White"
                    Background="Black"
                    BorderThickness="0"
                    Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <!--<Button x:Name="BackButton" Click="BackButton_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" Background="Transparent" BorderThickness="0" Margin="0,20,0,0" HorizontalAlignment="Center" Cursor="Hand">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE72B;" Style="{StaticResource BackIconStyle}"/>
                        <TextBlock Text="{x:Static language:Lang.GlobalButtonBack}" Foreground="Gray" FontSize="13"/>
                    </StackPanel>
                </Button>-->
            </StackPanel>


            <StackPanel x:Name="RegistrationCompletedPanel" Visibility="Collapsed" HorizontalAlignment="Center">
                <TextBlock Text="{x:Static language:Lang.UserSignUpLabelTitle}"  Style="{StaticResource TitleTextStyle}"/>
                <TextBlock Text="{x:Static language:Lang.UserSignUpLabelRegistrationComplete}" Style="{StaticResource SubtitleTextStyle}"/>

                <Grid Height="60" Width="60" Margin="0,30,0,25">
                    <Border Background="#28a745" CornerRadius="30"/>
                    <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
                <!--<Button x:Name="ContinueButton" Click="ContinueButton_Click" Content="{x:Static language:Lang.GlobalButtonContinue}" Margin="0,25,0,0" Padding="12" FontSize="16" FontWeight="Bold" Foreground="White" Background="Black" BorderThickness="0" Cursor="Hand">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>-->
            </StackPanel>

        </Grid>
    </Border>
</Window>
