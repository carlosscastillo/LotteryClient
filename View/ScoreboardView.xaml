<Page x:Class="Lottery.View.ScoreboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Lottery.View"
      mc:Ignorable="d"
      xmlns:language="clr-namespace:Lottery.Properties.Langs"
      d:DesignHeight="450" d:DesignWidth="800"
      Background="#F0F0F0"
      Title="ScoreboardView">

    <Page.Resources>
        <Style x:Key="LabelStyle" TargetType="Label">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style x:Key="StyledTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="#E0E0E0"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,0,10,0"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                            BorderBrush="{TemplateBinding BorderBrush}" 
                            BorderThickness="{TemplateBinding BorderThickness}" 
                            CornerRadius="5">
                            <ScrollViewer x:Name="PART_ContentHost" 
                                      Margin="0" 
                                      Padding="{TemplateBinding Padding}" 
                                      Background="Transparent"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#000000"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="180"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                            CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center"
                                          VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid>
        <StackPanel VerticalAlignment="Top" HorizontalAlignment="Center" Width="700">
            <TextBlock Text="Marcador" 
                   FontSize="28" 
                   FontWeight="Bold" 
                   TextAlignment="Center" 
                   Margin="0 0 0 60"/>

            <Grid HorizontalAlignment="Center" Width="478">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Left" Width="192">
                    <TextBlock Text="Posiciones" FontSize="16" FontWeight="Bold" Margin="0 0 0 20" TextAlignment="Left"/>
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                            <TextBlock Text="1." Width="30" HorizontalAlignment="Right"/>
                            <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="150"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                            <TextBlock Text="2." Width="30" HorizontalAlignment="Right"/>
                            <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="150"/>
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0 0 0 5">
                            <TextBlock Text="3." Width="30" HorizontalAlignment="Right"/>
                            <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="150"/>
                        </StackPanel>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,0" Width="229">
                    <TextBlock Text="Puntuación" FontSize="16" FontWeight="Bold" Margin="5 0 0 20" TextAlignment="Right" Width="184"/>
                    <StackPanel Width="229">
                        <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="100" HorizontalAlignment="Right" Margin="0 0 10 5"/>
                        <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="100" HorizontalAlignment="Right" Margin="0 0 10 5"/>
                        <TextBox Style="{StaticResource StyledTextBox}" IsReadOnly="True" Width="100" HorizontalAlignment="Right" Margin="0 0 10 5"/>
                    </StackPanel>
                </StackPanel>
            </Grid>

            <Button Content="Regresar"
                Style="{StaticResource PrimaryButtonStyle}"
                HorizontalAlignment="Center"
                Margin="0 100 0 0"/>
        </StackPanel>
    </Grid>
</Page>
